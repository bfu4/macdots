# /Library/LaunchDaemons
*a simple .plist for startup yabai*

this belongs in `/Library/LaunchDaemons/`

a simple plist to allow yabai to start on launch..
enable by 
``` launchctl start /Library/LaunchDaemons/bootstrap.yabai.plist ```
